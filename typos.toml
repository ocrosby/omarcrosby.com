[files]
extend-exclude = [
  "themes/",     # vendored theme, not ours to fix
  "public/",     # generated
  "resources/",  # generated
  "CHANGELOG.md",
  "data/music.yaml",  # YouTube video IDs look like typos to the checker
]

[default]
locale = "en-us"
